// var test = 'math';
// let one = test.charAt(0).toUpperCase();
// let two = test.slice(1);
// console.log(`${one}${two}`);


function cap(item) {
    let third = `${item.charAt(0).toUpperCase()}${item.slice(1)}`;
    console.log(third);
}

cap('scaryyyyyyyyy');